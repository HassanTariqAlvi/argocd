apiVersion: v1
kind: Service
metadata:
  name: {{ include "traefikIngressroute.fullname" . }}
  labels:
    {{- include "traefikIngressroute.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traefikIngressroute.selectorLabels" . | nindent 4 }}
